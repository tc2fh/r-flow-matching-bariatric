"""W6 journal figures for the MBSAQIP / Cosmos digital-twin project.

``figures.style`` owns the vector rcParams + palette + the calibration-caveat visual
language. ``figures.artifacts`` locates a frozen/eval run dir and loads its CSVs. The
``figure_*`` modules each build one figure; ``figures.build_all`` renders them all.
"""
